# Docker Compose 可选覆盖（放在项目根目录，文件名 .env）
# docker compose 会自动读取本文件中的变量插值

# 与官网 .env、xiaozhi-mcp/.env 中的 Token 保持一致
MCP_API_TOKEN=memory-harbor-mcp-dev-token

# 也可在此填写小智接入点（优先仍建议写在 xiaozhi-mcp/.env）
# XIAOZHI_MCP_ENDPOINT=wss://api.xiaozhi.me/mcp/?token=...
